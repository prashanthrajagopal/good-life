<div class = "row">
  <div class = "six columns">
<%= form_for @group do |f| %>
 <% if @group.errors.any? %>
    <div id="error-explanation">
      <p><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</p>
      <ul>
      <% @group.errors.full_messages.each do |msg| %>
        <li class = "error"><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= f.text_field :name, :placeholder => "Name" %>
    <%= f.text_area :description, :placeholder => "Description", :rows => "5" %>
    <%= f.submit controller.action_name == "edit"?"Update Group":"Create Group", :class => "button" %>
<% end %>

</div>
</div>